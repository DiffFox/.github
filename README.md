# DiffFox Organization Profile

This repository powers the public organization profile for `DiffFox` and can also store shared community files for organization-level defaults.

## Public Profile

The organization profile content lives in `profile/README.md`.
